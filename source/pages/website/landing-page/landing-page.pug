extends ../_layout.pug

block append variables
  - const pageTitle = 'Landing Page'

block append mixins
  include /blocks/card-search/card-search.pug

block content
  main.landing-page
    .landing-page__container
      .landing-page__item
        +card-search({ formAction: '#mock-address' })
      .landing-page__item
        p.landing-page__text
          | Лучшие номера для вашей работы, отдыха и просто вдохновения
